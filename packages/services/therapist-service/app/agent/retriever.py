class Retriever:
    """Stub retriever (for RAG)."""

    async def fetch(self, query: str) -> str:
        # Replace with vector DB / embeddings logic.
        return f"Retriever: No real context for '{query}', this is a stub."
